Acho que esse Rscript é o "Anotações" do R. 

Se for isso, será bem útil. 

1a observação: Usar o "Projects" é muito útil para organização. Basicamente, crio um diretório em que posso colocar tudo que preciso para um trabalho.
Nesse caso, posso criar um Project para cada aula. 

//Não sei como mudar nome de cada documento ainda kkkk mas vai indo. 

Cada aula terá um pequeno teste (Laboratório do Moodle), e no final da aula, alguém será sorteado para falar sobre. 

-- Comando "tecla Windows + setinha" para dividir a tela em duas janelas. Bem útil.

-- Função paste cria várias variáveis com nomes parecidos, tipo aluno1, aluno2, aluno3... facilmente.

-- Erro NaN significa "Not a Number".

-- min, max, sum, prod, operações... tudo isso lá nas anotações.

-- sort = função para colocar em ordem crescente ou decrescente.